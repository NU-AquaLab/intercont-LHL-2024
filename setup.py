from setuptools import setup, find_packages

setup(
    name="lhledge",
    author="Esteban Carisimo",
    version="0.1.0",
    # packages=["scn.utils"],
    # packages=find_packages(include=['src/utils/', ]),
    # py_modules=['scnUtils'],
    # package_dir={'': 'src/lhlcloud/'},
    # packages=["lhlcloud/", ],
    packages=find_packages(),
)
